# Base URL of the backend API. Leave blank in local development - Vite's
# dev server proxy (see vite.config.js) forwards /api requests to the
# backend automatically. Set this when the frontend is deployed separately
# from the backend (e.g. a Render/Vercel/Netlify deployment).
VITE_URL=
